export const metadata = {
  title: "Instagram Carousel Generator",
  description: "AI-powered carousel tool",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}